\documentclass{article}
\usepackage[a4paper,left=3.5cm,right=2.5cm,top=2.5cm,bottom=2.5cm]{geometry}
%%\usepackage[MeX]{polski}
%%\usepackage[cp1250]{inputenc}
\usepackage[utf8]{inputenc}
\usepackage[tableposition=top]{caption}
\usepackage{algorithmic}
\usepackage{graphicx}
\usepackage{enumerate}
\usepackage{multirow}
\usepackage{amsmath} %pakiet matematyczny
\usepackage{amssymb} %pakiet dodatkowych symboli
\usepackage{polski}
\begin{document}
$$\lim_{n\to\infty}
\sum_{k=1}^n \frac{1}{k^2}
=\frac{\pi^2}{6}$$


$$\prod_{i=2}^{n=i^{2}} = \frac{\lim^{n\to 4}(1+\frac{1}{n})^{n}}{\sum{k{\frac{1}{(n)}}}}$$

$$\int_{2}^{\infty} \frac{1}{log_{_2}x}dx = \frac{1}{x}{\sin{x}} = 1 - {\cos^2}{(x)}$$

$$
\left [\begin{array}{cccc}
a_{11}& a_{12} & \ldots & a_{1K}\\
\\ 
a_{21}& a_{22} &\ldots & a_{2K}\\
\vdots & \vdots & \ddots & \vdots\\
\\
a_{K1}& a_{K2} &\ldots & a_{KK}\\ 
\end{array}\right] * 
\left [\begin{array}{cccc} 
x_{1}\\
\\
x_{2}\\
\vdots\\
\\ 
 x_{K} 
\end{array}\right] 
= \left [\begin{array}{cccc}
b_{1}\\
\\
b_{2}\\
\vdots\\
\\
b_{K}
\end{array}\right]
$$

$$(a_{1} = a_{1}{(x))} \wedge (a_{2} = a_{2}{(x))} \wedge ... \wedge (a_{k} = a_{k}{(x))} \Rightarrow (d = d{(u))}$$


$$[{x}]_A = \{y\in U : a{(x)} = a{(y)}, \forall{a} \in A \} , where  the  central  object x \in U$$


$$ g{(u,r)} = \{\upsilon \in U : \frac{card\{IND(u,\upsilon)\}}{card\{A\}|} \geq r \}$$

$$ where, IND(u,\upsilon) = \{a \in A : a{(u)} = a{(\upsilon)}\}$$

$$ T : [0,1] \times [0,1] \to [0,1],$$

$$ \cos(2\theta) = \cos^{2} \theta - \sin^{2} \theta$$

$$ \lim_{n\to\infty} exp(-x) = 0$$


$$ \frac{n!}{k!(n - k)!} = {n \choose k}$$

$$ {P} \choose {A = 2 |\frac{A^2}{B} > 4}$$

$$ S^{c}_i(a) = \frac {\overline {C_i^a} - \hat {(C_i^a)} {Z _\overline {C_i^a^2} + {Z _\hat C_i^a^2}$$

$$ C_i^a = \{a{(u)} : u \in U and , d{(u)} = c_i \}$$

$$ A_{c_i}(a) = C_i^a \wedge_\varepsilon \{U \setminus C_i^a\}$$

$$ w{(u_q,\upsilon_p)} = w{(u_q,\upsilon_p)} + \frac{|a(u_q) - a(\upsilon_p)|}{(max attr_a - min attr_a) * \varepsilon}$$

$$
c'_ij = \left\{\begin{array}{ll}
c_i_j ,gdy,  d(x_i) \neq d(x_j)\\
\phi ,gdy, d(x_i) = d(x_j).\\
\end{array}\right.
$$






\end{document}
